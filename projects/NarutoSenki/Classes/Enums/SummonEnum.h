#pragma once
#include "Enums/Enums.hpp"

namespace SummonEnum
{
	// Kiba
	mk_const(Akamaru);

	// Deidara
	mk_const(Centipede);

	// Kakashi
	mk_const(DogWall);

	// Naruto
	mk_const(Kurama);

	// Tsunade
	mk_const(Slug);
	mk_const(SmallSlug);
} // namespace SummonEnum
