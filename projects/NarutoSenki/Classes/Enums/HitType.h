#pragma once
#include "Enums/Enums.hpp"

namespace HitType
{
	mk_const(n_hit);
	mk_const(b_hit);

	mk_const(l_hit);
	mk_const(f_hit);
	mk_const(f2_hit);
	mk_const(c_hit);
	mk_const(o_hit);
	mk_const(o2_hit);
	mk_const(ob_hit);
	mk_const(ab_hit);
	mk_const(ac_hit);
	mk_const(ct_hit);
	mk_const(ts_hit);
	mk_const(s_hit);

	mk_const(bl_hit);
	mk_const(bc_hit);
	mk_const(bf_hit);
	mk_const(sl_hit);
} // namespace HitType
